apiVersion: v1
kind: Service
metadata:
  name: retail-store-ui
  namespace: retail-app
spec:
  selector:
    app: retail-store-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
